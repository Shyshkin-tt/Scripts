using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ForTesting : MonoBehaviour
{
    public string _name;
    public string _location;
    
    public void SetName()
    {
        //InventoryHolder pname = GetComponentInParent<InventoryHolder>();
        //pname.CharacterStats.SetName(_name);
    }
    public void SetLoc()
    {
        //InventoryHolder ploc = GetComponentInParent<InventoryHolder>();
        //ploc.CharacterStats.SetLocation(_location);
    }
}

